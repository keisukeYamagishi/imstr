#!/usr/bin/python
import sys
from ImageConversion import ImageConversion
from extDetect import Extentions
from Progress import Indicator

if __name__ == "__main__":

    if Extentions(sys.argv).CanConversion() == True:
        im = ImageConversion(sys.argv[1])
        im.CreateHtml()
